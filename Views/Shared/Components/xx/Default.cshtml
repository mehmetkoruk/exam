@model List<Pmodel>
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Default</title>

</head>

<body>
    
    <h2>Başlıklar</h2>
        <select id="kategori" name="kategoriler">
          @foreach (var item in Model)
          {
              <option value="@item.PTitle">@item.PTitle</option>
          }
        </select>
    </div>

</body>
</html>
